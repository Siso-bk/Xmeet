# @meetx/shared

Shared types, Zod validators, and API contracts for MEETX.

## Local Development

Install as a file dependency from other repos:

```
npm install ../meetx-shared
```

Build:

```
npm run build
```

## Notes
- Build `@meetx/shared` before running the backend or frontend.
- Later you can publish to a private registry or GitHub Packages.
